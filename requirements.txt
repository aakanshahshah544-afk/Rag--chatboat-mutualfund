# Groww RAG Chatbot Backend - Vercel Optimized Dependencies
# Removed: selenium, playwright, webdriver-manager, beautifulsoup4, pandas, 
# schedule, apscheduler, pytest, chromadb (not needed for backend runtime)

# Core Web Framework
fastapi==0.110.0
uvicorn==0.28.0
pydantic==2.6.3
python-dotenv==1.0.1

# Data Processing (for embeddings & retrieval)
numpy==1.26.4
sentence-transformers==2.5.1
faiss-cpu==1.8.0

# LLM
groq==0.5.0
langchain==0.1.12
langchain-groq==0.0.1
langchain-community==0.0.29

# Utilities
requests==2.31.0
tqdm==4.66.2
python-dateutil==2.9.0
